#include<bits/stdc++.h>
using namespace std;
#define ll long long

ll sum_any_range(ll st, ll en, ll num){
	// num is number of numbers in this range
	return (num * (st + en) / 2);
}
int main() {
	 
}
