#include<bits/stdc++.h>
using namespace std;
#define ll long long 
 
int main() {
	int a, b, c; 
	cin >> a >> b >> c; 
	// a start range , c increasing variable , b value i search for 
	// for example : 1 7 3  >> 1 4 7 (7 is exist)
	// simply  : c*k + a == b ? is exist only when k is >=0 and integer number 
	// k = ( b - a ) / c 
}