  // compress array
    vector<int>compressed;
    set<int>st1;
    for (int i = 0; i < n; i++) {
        st1.insert(v[i]);
        if (st1.size() == 2) {
            st1.erase(st1.find(v[i]));
            compressed.push_back(*st1.begin());
            st1.clear();
            st1.insert(v[i]);
        }
    }